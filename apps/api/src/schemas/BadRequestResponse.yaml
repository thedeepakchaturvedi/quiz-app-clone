components:
    schemas:
        BadRequestResponse:
            type: object
            properties:
                message:
                    type: string
                    example: Error Message.
